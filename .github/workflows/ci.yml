name: CI
on:
  pull_request:
  push:
    branches: [main]

jobs:
  e2e:
    name: Build & E2E (Docker)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Prepare env
        run: cp .env.example .env

      - name: Build & start stack
        run: docker compose up --build -d

      - name: Run E2E suite (in container)
        run: docker compose --profile test run --rm e2e

      - name: Logs on failure
        if: failure()
        run: docker compose logs --no-color

      - name: Teardown
        if: always()
        run: docker compose down -v

  # ⏳ À venir : E2E web (Playwright) + mobile (Maestro) quand les fronts existeront — cf. transpo-testing.
