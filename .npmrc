# node-linker=hoisted : layout node_modules plat (façon npm), requis pour que
# l'autolinking Gradle de React Native / EAS résolve les modules dans un
# mono-repo pnpm (sinon "Included build '.../android/null' does not exist").
node-linker=hoisted
